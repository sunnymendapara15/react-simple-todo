# React Simple Todo

A minimal React todo app built with Vite. Add tasks, mark them as complete, and clear completed items with a single button.

## Getting started

```bash
npm install
npm run dev
```

The app will be available at `http://localhost:5173/` by default. Use `npm run build` to produce a production bundle.
